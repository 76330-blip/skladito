# 📋 ТЕХНИЧЕСКОЕ ЗАДАНИЕ
# **SKLADITO PHONE v2.0**

**Дата:** 30 января 2026  
**Версия:** 2.0 (Phone)  
**Статус:** Production Ready

---

## 1️⃣ ОПИСАНИЕ ПРОЕКТА

**Название:** SKLADITO Phone v2.0  
**Тип:** Локальное веб-приложение для учета хранимого барахла  
**Платформа:** Универсальная (любое устройство с браузером)  
**Технологии:** HTML5, CSS3, JavaScript (Vanilla JS)  
**Хранение:** LocalStorage браузера

**Назначение:** Учёт и организация предметов в контейнерах с неограниченной вложенностью на одном устройстве без необходимости сервера или интернета

---

## 2️⃣ АРХИТЕКТУРА

### **Однофайловое приложение:**

```
┌─────────────────────────────────────────┐
│  Устройство пользователя                │
│  ┌───────────────────────────────────┐  │
│  │  Браузер (Chrome/Safari/Firefox)  │  │
│  │  ┌─────────────────────────────┐  │  │
│  │  │  index-phone.html           │  │  │
│  │  │  - HTML структура           │  │  │
│  │  │  - CSS стили (встроенные)   │  │  │
│  │  │  - JavaScript логика        │  │  │
│  │  └─────────────────────────────┘  │  │
│  │           ↕                        │  │
│  │  ┌─────────────────────────────┐  │  │
│  │  │  LocalStorage               │  │  │
│  │  │  - skladito_containers      │  │  │
│  │  │  - skladito_items           │  │  │
│  │  │  - skladito_categories      │  │  │
│  │  └─────────────────────────────┘  │  │
│  └───────────────────────────────────┘  │
└─────────────────────────────────────────┘
```

### **Принципы работы:**

- **Offline-first:** Работает без интернета
- **Zero-install:** Не требует установки
- **Single-file:** Всё в одном HTML файле
- **Cross-platform:** Работает на любом устройстве

---

## 3️⃣ СТРУКТУРА ДАННЫХ

> **Важно:** Структура данных ИДЕНТИЧНА для всех веток SKLADITO!

### **КОНТЕЙНЕР**
```javascript
{
  id: string,              // Уникальный ID (формат: c1234567890)
  name: string,            // Название контейнера (обязательно)
  photo: string | null,    // Base64 фото (опционально)
  parent: string | null,   // ID родительского контейнера (null = корневой)
  created: string          // ISO 8601 дата создания
}
```

**Управление фото:**
- Можно добавить при создании
- Можно добавить/заменить при редактировании
- Можно удалить

---

### **ПРЕДМЕТ**
```javascript
{
  id: string,              // Уникальный ID (формат: i1234567890)
  name: string,            // Название предмета (обязательно)
  quantity: number,        // Количество (min: 1)
  category: string | null, // ID категории (опционально)
  photo: string | null,    // Base64 фото (опционально)
  container: string,       // ID контейнера (обязательно)
  created: string          // ISO 8601 дата создания
}
```

**Управление фото:**
- Можно добавить при создании
- Можно добавить/заменить при редактировании
- Можно удалить

---

### **КАТЕГОРИЯ**
```javascript
{
  id: string,    // Уникальный ID (формат: cat1234567890)
  name: string,  // Название категории
  icon: string,  // Эмодзи иконка (1-2 символа)
  order: number  // Порядок сортировки
}
```

**Предустановленные категории:**
- 🔧 Инструменты
- 📝 Канцелярия
- 🏠 Бытовые вещи
- ⚡ Электроника
- 👕 Одежда

---

## 4️⃣ ХРАНЕНИЕ ДАННЫХ

### **LocalStorage API:**

```javascript
// Сохранение
localStorage.setItem('skladito_containers', JSON.stringify(containers));
localStorage.setItem('skladito_items', JSON.stringify(items));
localStorage.setItem('skladito_categories', JSON.stringify(categories));
localStorage.setItem('skladito_version', '2.0');

// Загрузка
const containers = JSON.parse(localStorage.getItem('skladito_containers') || '[]');
const items = JSON.parse(localStorage.getItem('skladito_items') || '[]');
const categories = JSON.parse(localStorage.getItem('skladito_categories') || '[]');
```

### **Ограничения LocalStorage:**

- **Размер:** ~5-10 МБ (зависит от браузера)
- **Тип данных:** Только строки (требуется JSON.stringify/parse)
- **Безопасность:** Данные не зашифрованы
- **Очистка:** Данные удаляются при очистке браузера

### **Рекомендации по фото:**

- Сжимать изображения до 500 КБ
- Использовать JPEG вместо PNG
- Ограничить разрешение до 1024x1024px
- При приближении к лимиту - предупреждать пользователя

---

## 5️⃣ ФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ

> **Важно:** Функциональные требования ИДЕНТИЧНЫ для всех веток SKLADITO!

### ✅ **КОНТЕЙНЕРЫ (CRUD)**

**Создание:**
- [x] Создать с названием (обязательно)
- [x] Добавить фото (опционально, рекомендуется до 500 КБ)
- [x] Создать внутри другого контейнера
- [x] Создать корневой контейнер

**Просмотр:**
- [x] Просмотр содержимого
- [x] Навигация по иерархии (вход внутрь, возврат назад)
- [x] Хлебные крошки (Главная > Комната > Шкаф)
- [x] Количество объектов в контейнере

**Редактирование:**
- [x] Изменить название
- [x] Добавить фото
- [x] Заменить фото
- [x] Удалить фото

**Удаление:**
- [x] Удалить пустой контейнер
- [x] Блокировка удаления непустого контейнера
- [x] Предупреждение с указанием количества объектов внутри

---

### ✅ **ПРЕДМЕТЫ (CRUD)**

**Создание:**
- [x] Название + количество (обязательно)
- [x] Категория (опционально)
- [x] Фото (опционально, рекомендуется до 500 КБ)
- [x] Привязать к контейнеру

**Просмотр:**
- [x] В контейнере
- [x] По категориям
- [x] С иконкой категории
- [x] С количеством

**Редактирование:**
- [x] Название
- [x] Количество
- [x] Категория
- [x] Добавить фото
- [x] Заменить фото
- [x] Удалить фото

**Удаление:**
- [x] С подтверждением

---

### ✅ **КАТЕГОРИИ (Справочник)**

- [x] Просмотр списка категорий
- [x] Создание новой категории (название + иконка)
- [x] Удаление категории
- [x] Счётчик предметов в категории
- [x] Предупреждение при удалении категории с предметами
- [x] Предустановленные категории при первом запуске

---

### ✅ **ПОИСК И НАВИГАЦИЯ**

**Поиск:**
- [x] Поиск по названию контейнеров
- [x] Поиск по названию предметов
- [x] Поиск в реальном времени (при вводе)
- [x] Группировка результатов (Контейнеры / Предметы)

**Навигация:**
- [x] Кнопка "Назад" в заголовке
- [x] Хлебные крошки с полным путём
- [x] Клик по контейнеру для входа внутрь
- [x] Возврат на главную из любого места

**Фильтрация:**
- [x] Просмотр всех предметов в категории
- [x] Отображение пути к предмету при просмотре категории

---

### ✅ **ИНТЕРФЕЙС**

**Главный экран:**
- [x] Название: 📱 SKLADITO Phone v2.0
- [x] Индикация статуса: "Локальное хранилище"
- [x] 📊 Индикатор заполненности хранилища (уникально для Phone)
- [x] 📤📥 Кнопки экспорт/импорт (уникально для Phone)
- [x] Список корневых контейнеров
- [x] Поисковая строка
- [x] FAB кнопка с меню действий
- [x] **БЕЗ категорий** (категории доступны только через FAB меню)

**FAB Menu (при нажатии +):**
- [x] 📦 Создать контейнер
- [x] 📌 Создать предмет
- [x] 🏷️ Управление категориями

**Экран контейнера:**
- [x] Карточка текущего контейнера (с возможностью редактирования)
- [x] Список вложенных контейнеров
- [x] Список предметов в контейнере
- [x] Пустое состояние (если контейнер пуст)

**Модальные окна:**
- [x] Создание контейнера
- [x] Создание предмета
- [x] Редактирование контейнера
- [x] Редактирование предмета
- [x] Управление категориями

---

### ✅ **ДОПОЛНИТЕЛЬНЫЕ ВОЗМОЖНОСТИ**

**Экспорт/Импорт данных:**
- [x] Экспорт всех данных в JSON файл
- [x] Импорт данных из JSON файла
- [x] Резервное копирование перед импортом

**Статистика:**
- [ ] Не показывается (убрана по ТЗ)

**Индикатор заполненности хранилища:**
- [x] Показать использование LocalStorage
- [x] Предупреждение при приближении к лимиту (>80%)
- [x] Рекомендации по оптимизации (сжатие фото)

---

## 6️⃣ ТЕХНИЧЕСКИЕ ДЕТАЛИ

### **Сжатие изображений:**

```javascript
function compressImage(file, maxSizeKB = 500) {
    return new Promise((resolve) => {
        const reader = new FileReader();
        reader.onload = (e) => {
            const img = new Image();
            img.onload = () => {
                const canvas = document.createElement('canvas');
                let width = img.width;
                let height = img.height;
                
                // Ограничение разрешения
                const maxDimension = 1024;
                if (width > maxDimension || height > maxDimension) {
                    if (width > height) {
                        height = (height / width) * maxDimension;
                        width = maxDimension;
                    } else {
                        width = (width / height) * maxDimension;
                        height = maxDimension;
                    }
                }
                
                canvas.width = width;
                canvas.height = height;
                
                const ctx = canvas.getContext('2d');
                ctx.drawImage(img, 0, 0, width, height);
                
                // Сжатие JPEG
                let quality = 0.8;
                let result = canvas.toDataURL('image/jpeg', quality);
                
                // Проверка размера
                while (result.length > maxSizeKB * 1024 && quality > 0.1) {
                    quality -= 0.1;
                    result = canvas.toDataURL('image/jpeg', quality);
                }
                
                resolve(result);
            };
            img.src = e.target.result;
        };
        reader.readAsDataURL(file);
    });
}
```

### **Проверка размера LocalStorage:**

```javascript
function getStorageUsage() {
    let total = 0;
    for (let key in localStorage) {
        if (localStorage.hasOwnProperty(key)) {
            total += localStorage[key].length + key.length;
        }
    }
    return {
        used: total,
        usedMB: (total / 1024 / 1024).toFixed(2),
        percent: Math.round((total / (5 * 1024 * 1024)) * 100)
    };
}
```

### **Экспорт данных:**

```javascript
function exportData() {
    const data = {
        version: '2.0',
        exportDate: new Date().toISOString(),
        containers: JSON.parse(localStorage.getItem('skladito_containers') || '[]'),
        items: JSON.parse(localStorage.getItem('skladito_items') || '[]'),
        categories: JSON.parse(localStorage.getItem('skladito_categories') || '[]')
    };
    
    const blob = new Blob([JSON.stringify(data, null, 2)], {
        type: 'application/json'
    });
    const url = URL.createObjectURL(blob);
    const a = document.createElement('a');
    a.href = url;
    a.download = `skladito-backup-${Date.now()}.json`;
    a.click();
}
```

### **Импорт данных:**

```javascript
function importData(file) {
    const reader = new FileReader();
    reader.onload = (e) => {
        try {
            const data = JSON.parse(e.target.result);
            
            // Проверка версии
            if (data.version !== '2.0') {
                alert('Неподдерживаемая версия данных');
                return;
            }
            
            // Резервное копирование текущих данных
            const backup = {
                containers: localStorage.getItem('skladito_containers'),
                items: localStorage.getItem('skladito_items'),
                categories: localStorage.getItem('skladito_categories')
            };
            localStorage.setItem('skladito_backup', JSON.stringify(backup));
            
            // Импорт
            localStorage.setItem('skladito_containers', JSON.stringify(data.containers));
            localStorage.setItem('skladito_items', JSON.stringify(data.items));
            localStorage.setItem('skladito_categories', JSON.stringify(data.categories));
            
            location.reload();
        } catch (error) {
            alert('Ошибка импорта: ' + error.message);
        }
    };
    reader.readAsText(file);
}
```

---

## 7️⃣ УСТАНОВКА И ИСПОЛЬЗОВАНИЕ

### **Установка:**

1. Скачать файл `index-phone.html`
2. Открыть в браузере (двойной клик)
3. Готово! Приложение работает

### **Рекомендуемые браузеры:**

- ✅ Chrome/Chromium (любая версия)
- ✅ Safari (iOS/macOS)
- ✅ Firefox (любая версия)
- ✅ Edge (Chromium-based)

### **Добавление на главный экран (PWA-style):**

**iOS Safari:**
1. Открыть index-phone.html в Safari
2. Нажать "Поделиться"
3. "На экран «Домой»"
4. Готово! Работает как приложение

**Android Chrome:**
1. Открыть index-phone.html в Chrome
2. Меню → "Добавить на главный экран"
3. Готово! Работает как приложение

---

## 8️⃣ ОГРАНИЧЕНИЯ И РЕКОМЕНДАЦИИ

### **Ограничения:**

- ❌ Нет синхронизации между устройствами
- ❌ Нет резервного копирования (кроме ручного экспорта)
- ❌ Данные удаляются при очистке браузера
- ❌ Размер хранилища ограничен (~5-10 МБ)
- ❌ Нет работы в инкогнито-режиме

### **Рекомендации:**

- ✅ Регулярно делать экспорт данных (резервные копии)
- ✅ Сжимать фото перед добавлением
- ✅ Следить за заполненностью хранилища
- ✅ Не использовать режим инкогнито
- ✅ Использовать современные браузеры

### **Миграция на LAN/Cloud:**

Если данных станет много или нужна синхронизация:
1. Экспортировать данные из Phone версии
2. Развернуть LAN или Cloud версию
3. Импортировать данные через API

---

## 9️⃣ ОТЛИЧИЯ ОТ ДРУГИХ ВЕТОК

| Параметр | PHONE | LAN | CLOUD |
|----------|-------|-----|-------|
| Установка | Открыть файл | Docker | Cloud deploy |
| Хранение | LocalStorage | MongoDB (NAS) | MongoDB (облако) |
| Синхронизация | Нет | Да | Да |
| Размер данных | ~10 МБ | Неограничен | Неограничен |
| Offline | Да | Нет | Нет |
| Резервное копирование | Ручное | Автоматическое | Автоматическое |
| Доступ | Одно устройство | Локальная сеть | Интернет |

---

## 🔟 ФАЙЛОВАЯ СТРУКТУРА

```
index-phone.html (единственный файл!)
├── HTML структура
├── <style> - все CSS встроены
└── <script> - вся логика JavaScript
    ├── State management
    ├── LocalStorage API
    ├── UI rendering
    ├── Event handlers
    ├── Image compression
    ├── Export/Import
    └── Initialization
```

---

## 📞 КОНТАКТЫ И ПОДДЕРЖКА

**Разработчик:** SKLADITO Team  
**Версия ТЗ:** 2.0 (Phone)  
**Дата последнего обновления:** 30 января 2026

---

**КОНЕЦ ДОКУМЕНТА**
