# 📋 ТЕХНИЧЕСКОЕ ЗАДАНИЕ
# **SKLADITO - Обзор проекта**

**Дата:** 30 января 2026  
**Версия:** 2.0  
**Статус:** Production

---

## 🎯 ОБЩЕЕ ОПИСАНИЕ

**SKLADITO** - семейство веб-приложений для учета хранимого барахла с поддержкой трёх режимов развертывания.

**Общая функциональность для всех веток:**
- Контейнеры с неограниченной вложенностью
- Предметы с количеством и категориями
- Фото для контейнеров и предметов
- Поиск по названию
- Управление категориями

---

## 📱 SKLADITO PHONE v2.0

### **Описание:**
Локальная версия для работы на одном устройстве без сервера.

### **Технологии:**
- HTML5 + JavaScript (Vanilla)
- LocalStorage для хранения данных
- Работает offline

### **Архитектура:**
```
┌─────────────────────────────┐
│  Браузер на устройстве      │
│  - HTML/CSS/JS              │
│  - LocalStorage (~10MB)     │
└─────────────────────────────┘
```

### **Развертывание:**
- Открыть HTML файл в браузере
- Никаких серверов не требуется

### **Ограничения:**
- Данные только на одном устройстве
- Нет синхронизации
- Размер данных ограничен браузером (~10MB)
- Нет резервного копирования

### **Преимущества:**
- Максимально простая установка
- Работает везде (даже offline)
- Нет зависимостей от сети/сервера

### **Файлы:**
- `index-phone.html` - всё приложение в одном файле

---

## 🏠 SKLADITO LAN v2.0

### **Описание:**
Серверная версия для работы в локальной сети с общей базой данных.

### **Технологии:**
- **Frontend:** HTML5 + JavaScript (Vanilla)
- **Backend:** Node.js + Express
- **Database:** MongoDB
- **Deployment:** Docker на Synology NAS

### **Архитектура:**
```
┌─────────────────────────────┐
│  Любое устройство в сети    │
│  - Браузер                  │
└──────────────┬──────────────┘
               │ HTTP
               ↓
┌─────────────────────────────┐
│  Nginx (порт 8080)          │
│  - Статика + API Proxy      │
└──────────────┬──────────────┘
               │
               ↓
┌─────────────────────────────┐
│  Node.js API (порт 3000)    │
│  - REST endpoints           │
└──────────────┬──────────────┘
               │
               ↓
┌─────────────────────────────┐
│  MongoDB (порт 27017)       │
│  - Общая база данных        │
└─────────────────────────────┘
```

### **Развертывание:**
- Docker контейнеры на NAS
- 3 контейнера: MongoDB, API, Nginx
- Доступ по `http://IP:8080` в локальной сети

### **Ограничения:**
- Доступ только в локальной сети
- Требует NAS или сервер
- Нет авторизации пользователей

### **Преимущества:**
- Синхронизация между устройствами
- Неограниченный размер данных
- Автоматическое резервное копирование (MongoDB)
- Одновременная работа нескольких пользователей

### **Файлы:**
- `index-lan.html` - фронтенд
- `server.js` - API сервер
- `package.json` - зависимости
- `nginx.conf` - конфигурация

---

## ☁️ SKLADITO CLOUD v2.0

### **Описание:**
Облачная версия с доступом через интернет и системой авторизации.

### **Технологии:**
- **Frontend:** HTML5 + JavaScript (Vanilla)
- **Backend:** Node.js + Express
- **Database:** MongoDB Atlas (облако)
- **Auth:** JWT токены
- **Deployment:** Cloud hosting (Vercel/Railway/AWS)

### **Архитектура:**
```
┌─────────────────────────────┐
│  Любое устройство           │
│  - Браузер (интернет)       │
└──────────────┬──────────────┘
               │ HTTPS
               ↓
┌─────────────────────────────┐
│  Cloud Frontend             │
│  - CDN + Static hosting     │
└──────────────┬──────────────┘
               │
               ↓
┌─────────────────────────────┐
│  Cloud API                  │
│  - REST + Auth JWT          │
└──────────────┬──────────────┘
               │
               ↓
┌─────────────────────────────┐
│  MongoDB Atlas              │
│  - Multi-tenant DB          │
└─────────────────────────────┘
```

### **Дополнительные возможности:**
- **Авторизация:**
  - Регистрация/вход пользователей
  - JWT токены
  - Сессии

- **Выделенные хранилища:**
  - Каждый пользователь создаёт свои хранилища
  - Приглашение других пользователей
  - Разграничение прав доступа (владелец/редактор/читатель)

- **Безопасность:**
  - HTTPS обязательно
  - Шифрование паролей (bcrypt)
  - Защита от CSRF/XSS

### **Развертывание:**
- Cloud hosting (Vercel/Railway/AWS)
- MongoDB Atlas
- Доступ по `https://skladito.app`

### **Ограничения:**
- Требует интернет
- Платные облачные сервисы
- Сложнее в развертывании

### **Преимущества:**
- Доступ откуда угодно
- Авторизация пользователей
- Выделенные хранилища
- Совместная работа
- Автоматические обновления

### **Файлы:**
- `index-cloud.html` - фронтенд с авторизацией
- `server-cloud.js` - API с auth
- `auth.js` - модуль авторизации
- Конфигурации для деплоя

---

## 📊 СРАВНЕНИЕ ВЕТОК

| Параметр | PHONE | LAN | CLOUD |
|----------|-------|-----|-------|
| **Хранение** | LocalStorage | MongoDB на NAS | MongoDB Atlas |
| **Доступ** | Одно устройство | Локальная сеть | Интернет |
| **Синхронизация** | Нет | Да | Да |
| **Авторизация** | Нет | Нет | Да |
| **Размер данных** | ~10MB | Неограничен | Неограничен |
| **Установка** | Открыть файл | Docker | Cloud deploy |
| **Сложность** | Простая | Средняя | Сложная |
| **Стоимость** | Бесплатно | Требует NAS | Требует облако |
| **Offline работа** | Да | Нет | Нет |
| **Резервное копирование** | Ручное | Автоматическое | Автоматическое |

---

## 🔄 МИГРАЦИЯ МЕЖДУ ВЕТКАМИ

### **PHONE → LAN:**
- Экспорт данных из LocalStorage в JSON
- Импорт в MongoDB через API

### **LAN → CLOUD:**
- Экспорт базы MongoDB
- Импорт в MongoDB Atlas
- Добавление авторизации

### **PHONE → CLOUD:**
- Экспорт + импорт через JSON
- Регистрация пользователя

---

## 🎯 ПРИОРИТЕТ РАЗРАБОТКИ

### **Статус текущий:**
1. ✅ **LAN v2.0** - Production ready
2. ⏳ **PHONE v2.0** - В разработке
3. 📋 **CLOUD v2.0** - Планируется

---

## 📋 ОБЩИЕ ФУНКЦИОНАЛЬНЫЕ ТРЕБОВАНИЯ

Все три ветки имеют единый набор функций (см. детальные ТЗ для каждой ветки):

### **Контейнеры:**
- Создание/редактирование/удаление
- Неограниченная вложенность
- Фото (добавить/заменить/удалить)
- Навигация по иерархии

### **Предметы:**
- Создание/редактирование/удаление
- Количество
- Категории
- Фото (добавить/заменить/удалить)

### **Категории:**
- Справочник категорий
- Создание/удаление
- Иконки (эмодзи)

### **Интерфейс:**
- Поиск
- Хлебные крошки
- FAB меню
- Адаптивный дизайн

---

## 📄 ДЕТАЛЬНЫЕ ТЗ

- **TZ-PHONE-v2.0.md** - Детальное ТЗ для PHONE версии
- **TZ-LAN-v2.0.md** - Детальное ТЗ для LAN версии
- **TZ-CLOUD-v2.0.md** - Детальное ТЗ для CLOUD версии

---

**КОНЕЦ ДОКУМЕНТА**
